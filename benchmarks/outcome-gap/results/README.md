# Generated benchmark results

Run `bun run benchmark:outcome-gap` from the repository root. Reproducible evidence, signatures,
and the release decision are written to `.invokesmith/outcome-gap/`, which is intentionally ignored
because the bundle includes an ephemeral demonstration private key and environment-specific runtime
identities.

The stable expected verdicts are committed in the case's `expected-results.json` file.
